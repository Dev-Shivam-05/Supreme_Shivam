# ============================================================
#  Shivam Bhadoriya — Portfolio
#  Copy this file to `.env.local` and fill in what you need.
#
#  The site runs fine with everything blank. Each feature turns
#  on the moment you provide its variables — nothing else to change.
# ============================================================

# --- Database: MongoDB --------------------------------------
# Turns on: contact-form lead storage, the analytics pipeline,
#           the public /stats page, and the /admin dashboard data.
# Free cluster: https://www.mongodb.com/atlas  (use the SRV string)
MONGODB_URI=

# --- Email: SMTP (via Nodemailer) ---------------------------
# Turns on: emailing contact-form leads to you.
# Works with a Gmail App Password, Resend SMTP, Mailgun, Postmark, etc.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# The "from" address (defaults to SMTP_USER if left blank)
SMTP_FROM=
# Where lead notifications are delivered (defaults to contact@shivambhadoriya.com)
CONTACT_TO=

# --- Analytics salt ------------------------------------------------------
# Used to hash visitor IPs instead of storing them. The hash also mixes in the
# UTC date, so it rotates daily and yesterday's visits cannot be joined to
# today's — that is what keeps the site cookie-free and consent-free.
#
# It works without this set, but then the salt is a known constant and the hash
# could in principle be brute-forced back to an IP. Set it in production:
#   openssl rand -hex 32
ANALYTICS_SALT=

# --- Admin dashboard: /admin --------------------------------
# Turns on: the analytics command center + leads pipeline login.
# ADMIN_PASSWORD  -> the password you'll type at /admin
# ADMIN_SECRET    -> a long random string used to sign the session cookie
#                    (generate: `openssl rand -hex 32`)
ADMIN_PASSWORD=
ADMIN_SECRET=

# --- Canonical origin --------------------------------------------------------
# The public origin the site is served from. Leave unset (or pointed at the
# vercel.app host) until an exact-match domain is live — setting it to a domain
# that does not resolve makes next.config.ts 301 the live site into a dead host,
# and a 301 is cached hard by browsers.
#
# Once shivambhadoriya.com is pointed at this Vercel deployment, set:
#   NEXT_PUBLIC_SITE_URL=https://shivambhadoriya.com
# Everything downstream follows: metadata, canonicals, JSON-LD, OG URLs, the
# sitemap, robots.txt, and the 301 from the old vercel.app host.
NEXT_PUBLIC_SITE_URL=
